import { useState } from "react"; export function Disclosure({ title, children }: { title: string; children: React.ReactNode }) { const [isOpen, setIsOpen] = useState(false); return ( <div style={{ margin: "20px 0", border: "1px solid #444", borderRadius: "6px", overflow: "hidden" }}> <button type="button" aria-expanded={isOpen} onClick={() => setIsOpen(!isOpen)} style={{ width: "100%", padding: "14px 20px", display: "flex", justifyContent: "space-between", alignItems: "center", backgroundColor: "#222", color: "white", border: "none", cursor: "pointer", fontSize: "16px", textAlign: "left", fontWeight: "bold" }} > <span>{title}</span> <span>{isOpen ? "?" : "?"}</span> </button> <div hidden={!isOpen} style={{ padding: "20px", backgroundColor: "#1a1a1a", color: "#ccc", borderTop: "1px solid #444", display: isOpen ? "block" : "none" }} > {children}</div> </div> ); }
