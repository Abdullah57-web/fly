import { useState } from "react"; import { Modal } from "./playground/Modal"; import { Tabs } from "./playground/Tabs"; import { Disclosure } from "./playground/Disclosure"; export default function App() { const [isModalOpen, setIsModalOpen] = useState(false); const tabData = [ { id: "tab1", label: "Overview", content: "This playground displays custom hand-crafted accessible interactive widgets." }, { id: "tab2", label: "ARIA Specs", content: "Keyboard patterns match official W3C structural definitions for assistive software." }, { id: "tab3", label: "Evaluation", content: "Next up we will build a disclosure, then compare our work directly against shadcn." } ]; return ( <div style={{ padding: "40px", fontFamily: "sans-serif", maxWidth: "600px", margin: "0 auto" }}> <h1>Accessibility Playground</h1> <button onClick={() => setIsModalOpen(true)} style={{ padding: "10px 20px", fontSize: "16px", cursor: "pointer", marginBottom: "20px" }}> Open Custom Modal </button> <Tabs items={tabData} /> <Disclosure title="Click to view Assignment Instructions"> <p>Build a playground containing:</p> <ul style={{ paddingLeft: "20px" }}> <li>A focus-trapped Modal dialog</li> <li>An arrow-key navigable Tabs widget</li> <li>A semantic show/hide Disclosure component</li> </ul> </Disclosure> <Modal isOpen={isModalOpen} onClose={() => setIsModalOpen(false)} title="My Custom Accessible Modal"> <p>Try navigating using only your keyboard!</p> </Modal> </div> ); }
